package ui.menu;

import javax.swing.*;
import java.awt.*;

/**
 * Custom MenuBar
 */
public class MenuBar extends JPanel {
    public MenuBar() {
        init();
    }

    private void init() {
        setBackground(Color.BLACK);
        setVisible(true);
    }
}
